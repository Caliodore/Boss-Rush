using UnityEngine;

namespace Caliodore
{ 
    public class P1_SpawnManager : MonoBehaviour
    {
        /*
         * Script to negotiate actually spawning enemies and where to spawn them.
         * Needs methods for checking current amount of enemies (through EnemyStorage), whether or not a chosen is out, and finding and storing spawn points.
         */
    }
}
